# Base + dev tooling (pre-commit, ruff, mypy). Sync with [project.optional-dependencies].dev.
# Install: uv pip install -r requirements/dev.txt && uv pip install -e ".[dev]"
# Or: uv sync --extra dev
-r base.txt
pre-commit>=4.1.0
ruff>=0.9.2
mypy>=1.11.2
