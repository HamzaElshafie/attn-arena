# Runtime dependencies — keep in sync with [project.dependencies] in pyproject.toml.
# Install: uv pip install -r requirements/base.txt && uv pip install -e .
# Or use: uv sync (from project root).
torch>=2.0.0
pydantic>=2.0
transformers>=4.36.0
